name: Publish NuGet Package

on:
  push:
    tags: [ 'v*' ]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          path: ControlAgentNet.Channels.Console

      - uses: actions/checkout@v4
        with:
          repository: ControlAgentNet/ControlAgentNet.Agents
          path: ControlAgentNet.Agents

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Read base version
        id: base_version
        shell: bash
        working-directory: ControlAgentNet.Channels.Console
        run: |
          BASE_VERSION=$(python - <<'PY'
          import re
          from pathlib import Path
          text = Path('Directory.Build.props').read_text()
          match = re.search(r'<VersionPrefix>([^<]+)</VersionPrefix>', text)
          if not match:
              raise SystemExit('VersionPrefix not found in Directory.Build.props')
          print(match.group(1))
          PY
          )
          echo "base_version=${BASE_VERSION}" >> "$GITHUB_OUTPUT"

      - name: Validate release tag matches VersionPrefix
        shell: bash
        working-directory: ControlAgentNet.Channels.Console
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          BASE_VERSION="${{ steps.base_version.outputs.base_version }}"

          if [[ "$TAG_VERSION" != "$BASE_VERSION" ]]; then
            echo "Release tag version ($TAG_VERSION) does not match VersionPrefix ($BASE_VERSION)."
            exit 1
          fi

      - name: Restore
        working-directory: ControlAgentNet.Channels.Console
        run: dotnet restore ControlAgentNet.Channels.Console.slnx

      - name: Build
        working-directory: ControlAgentNet.Channels.Console
        run: dotnet build ControlAgentNet.Channels.Console.slnx --no-restore -c Release /p:Version=${GITHUB_REF_NAME#v}

      - name: Pack
        working-directory: ControlAgentNet.Channels.Console
        run: dotnet pack ControlAgentNet.Channels.Console.slnx --no-build -c Release -o artifacts/nuget /p:Version=${GITHUB_REF_NAME#v}

      - name: Push NuGet packages
        shell: bash
        working-directory: ControlAgentNet.Channels.Console
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: |
          dotnet nuget push "artifacts/nuget/*.nupkg" --source "https://api.nuget.org/v3/index.json" --api-key "$NUGET_API_KEY" --skip-duplicate
          dotnet nuget push "artifacts/nuget/*.snupkg" --source "https://api.nuget.org/v3/index.json" --api-key "$NUGET_API_KEY" --skip-duplicate
